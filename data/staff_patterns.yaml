# 员工名称识别规则
# 匹配以下任一规则的发言者被视为「员工」，其余视为「客户」
# 支持正则表达式和白名单两种模式

# 正则规则 — 匹配则视为员工
regex:
  - ".*@.*"           # 名称中包含 @ (如"张三@公司名")
  - ".*客服.*"         # 名称中包含"客服"

# 白名单 — 精确匹配视为员工
whitelist:
  # - "张三"
  # - "李四"

# 黑名单 — 精确匹配强制视为客户（优先级高于正则和白名单）
blacklist: []
