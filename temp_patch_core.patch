*** Begin Patch
*** Update File: app\master\core.py
@@
    async def dispatch_send_tasks(self, active_groups: list):
-        """根据活跃群列表向在线执行端分配发送任务。"""
+        """根据活跃群列表向在线执行端分配发送任务。
+
+        Args:
+            active_groups: 目标群名称列表
+            extra: 额外配置（节假日模板、延迟覆盖等）
+        """
+    async def dispatch_send_tasks(self, active_groups: list, extra: dict = None):
@@
+        extra = extra or {}
+        is_holiday = extra.get("is_holiday", False)
+
@@
              task_data = {
                  "type": "send_task",
                  "task_id": task_id,
                  "groups": groups,
-                  "template": "default.j2",
+                  "template": extra.get("template", "default.j2"),
              }
@@
-            task_data = {
-                "type": "send_task",
-                "task_id": task_id,
-                "groups": groups,
-                "template": "default.j2",
-            }
+            task_data = {
+                "type": "send_task",
+                "task_id": task_id,
+                "groups": groups,
+                "template": extra.get("template", "default.j2"),
+                "delay_min": extra.get("delay_min"),
+                "delay_max": extra.get("delay_max"),
+                "is_holiday": is_holiday,
+            }
@@
+    async def dispatch_auto_reply(self, group_name: str, reply_text: str):
+        """下发自动回复任务给负责该群的执行端。"""
+        worker_id = self._group_assignment.get(group_name)
+        if not worker_id:
+            logger.warning(f"自动回复失败: {group_name} 未分配执行端")
+            return
+
+        if worker_id not in self._workers:
+            logger.warning(f"自动回复失败: {worker_id} 未注册")
+            return
+
+        if self._workers[worker_id].get("status") != "online":
+            online = [w for w in self._workers if self._workers[w].get("status") == "online"]
+            if online:
+                worker_id = online[0]
+            else:
+                logger.warning("自动回复失败: 无在线执行端")
+                return
+
+        task_data = {
+            "type": "auto_reply",
+            "group_name": group_name,
+            "reply_text": reply_text,
+        }
+        await self._send_to_worker(worker_id, task_data)
+        logger.info(f"自动回复任务已下发: {group_name} → {worker_id}")
+
*** End Patch
